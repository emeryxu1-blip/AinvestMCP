# AInvest MCP 原型方案

> 内部评审稿 · 2026-09-15。下文区分当前实测与未来目标；本次交付为方案和前端演示，后端改造后续推进。

**连接更简单，数据更完整，内部与外部共用一套 MCP。**

## 1. 当前问题

**当前实测：现有 MCP 已验证证券查询可用，返回 11 个工具；仍存在查询接口异常，分析师评级返回服务端 500。** 工具发现成功不等于认证成功，也不代表所有查询可用。

- 初次连接要找密钥、拼配置、判断错误，用户难以知道是否真正连通。
- AI 接入体验不完整：官网已有 [llms.txt](https://docs.ainvest.com/llms.txt)，但文档、工具说明与实际接口仍需同步，减少 AI 反复查找和尝试。
- 外部 MCP 的能力覆盖落后于内部 Index API，用户在不同入口获得的数据能力不一致。
- 内部通过 skills 接数据、外部通过 MCP，新增能力与修复需要重复适配。

## 2. 连接体验

**目标：选择客户端 → 完成连接 → 成功查询第一条数据。** 用户按客户端指引完成连接，再用一次真实查询确认可用；页面说明当前进度和下一步。

密钥错误时提示检查或更换密钥；服务异常时说明该查询暂不可用并提供重试，保留已完成的连接步骤，不把失败显示成“无数据”。

原型以固定示例演示三步流程，可切换正常连接、密钥错误、服务异常；不收集真实密钥，不调用真实服务，演示成功不代表已连接用户账号。

## 3. 统一 MCP

**未来目标：内部 AI / 外部 AI → 同一套 MCP → Index API 等底层数据服务。** 按账号权限提供能力，统一发现、调用与错误反馈，让用户换入口也能延续相同的使用方式。

逐步开放全部 Index API 能力，包括指标、历史变化、行业成分、ETF 持仓等，不以现有已封装接口为终点。统一维护持续更新的能力目录、参数说明、调用示例和数据更新时间，让 AI 找得到、调得对，也能判断结果是否适用。

skills 如保留，只编排分析流程，数据统一通过 MCP 获取。新增或修复一次，内部与外部同步受益，只维护一套数据调用协议。

## 4. 推进步骤

1. **改善连接与文档：** 先让用户顺利完成首条查询，错误有解释，指引与实际能力一致。
2. **内部调用统一迁入 MCP：** 将 skills 中的数据调用迁移到同一入口，验证内部常用流程。
3. **扩展至全部 Index API 能力：** 持续补齐覆盖并同步目录，清楚标注已开放和待开放能力。

验收以体验为准：用户两分钟内理解改版方向；桌面和手机都能走通演示；密钥错误与服务异常有清晰下一步；当前实测与未来目标不混淆。
